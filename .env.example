# Инфраструктурные переменные — всё остальное настраивается в /admin/settings

PORT=3000
PUBLIC_URL=https://bot.example.com

# Админка (basic auth)
ADMIN_LOGIN=admin
ADMIN_PASSWORD=changeme

# Необязательное — переопределение API URL Platega (например, для testnet)
# PLATEGA_API_URL=https://app.platega.io
